#Conectar-se a VM

>ssh 'publicIpAddress'

#Conectar-se com o banco de dados com as credenciais
#Substituir [password] por uma senha escolhida por vocÊ
> sqlcmd -S tcp:serverNNNN.database.windows.net,1433 -d marketplaceDb -U '[]' -P '[password]' -N -l 30

#Por padrão o acesso ao banco de dados vem restrito e é necessário autorizá-lo por meio do portal Azure
